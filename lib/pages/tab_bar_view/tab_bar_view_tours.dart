import 'package:flutter/material.dart';

class TabBarViewTours extends StatelessWidget {
  const TabBarViewTours({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tours Menu'),
    );
  }
}
