import 'package:flutter/material.dart';

class TabBarViewAdventures extends StatelessWidget {
  const TabBarViewAdventures({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Adventures Menu'),
    );
  }
}
